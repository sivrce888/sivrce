/** Shared catchments for nearest-POI (kept tiny — no import of JSON). */

/** ~10 min walk — RE “near metro” filter default (matches MAP_POIS METRO_NEAR_M). */
export const METRO_NEAR_M = 800

/** Max distance to store a nearest-POI link. */
export const METRO_MAX_CATCHMENT_M = 2500
