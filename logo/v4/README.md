# Sivrce — Logo v4a · "Aurora Spark" (LIVE)

Board-faithful mark: connected four-point star + star void, brand flow
gradient landing on the orange tip. Owner board → vector, no raster gloss
in the React mark (display SVGs keep light vector sheen).

```bash
python3 build_v4.py      # regenerate masters (a/b/c variants)
python3 ship_aurora.py   # render + copy into app/ (favicon, icons, OG, public/logo)
```

Live wiring: `app/src/components/Logo.tsx` · `app/src/app/icon.svg` ·
`apple-icon.png` · `favicon.ico` · `public/icons/*` · `public/images/og.jpg`.

v4b / v4c = experiments only. v3 blades = history in `logo/v3/`.
